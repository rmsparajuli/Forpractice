// object to string

// lets define

const dt = new Date();
console.log(`original date is ${dt}`);

console.log(`and the string date is ${dt.toString()}`); //we use to () after toString because it is method
console.log(`typeof of original date ${typeof dt}`);
console.log(`typeof of string date is ${typeof dt.toString()}`);
